namespace BCA.CarAuctionManagement.Infrastructure.Settings;

internal static class Constants
{
    internal static class Dependencies
    {
        public const string SqlServer = $"{nameof(Dependencies)}:{nameof(SqlServer)}";
    }
}
